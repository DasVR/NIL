{
  "name": "NIL — Finn pentest workstation",
  "install": "./.cursor/install.sh",
  "terminals": [
    {
      "name": "api",
      "command": "FINN_API_HOST=0.0.0.0 python3 main.py",
      "description": "Finn backend — FastAPI + WebSocket API on :8766 (serves /v1)."
    },
    {
      "name": "web",
      "command": "cd frontend && npm run dev -- --host 0.0.0.0 --port 5173",
      "description": "NIL SvelteKit dev server on :5173 (HMR). Proxies /v1 and WS to the API on :8766."
    }
  ],
  "ports": [
    { "name": "web", "port": 5173 },
    { "name": "api", "port": 8766 }
  ]
}
