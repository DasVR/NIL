# Ollama Cloud (provider)
OLLAMA_CLOUD_API_KEY=oc-...
OLLAMA_CLOUD_BASE=https://api.ollama-cloud.com/v1

# API auth (optional — set to require Bearer token)
GODMODE_API_KEY=change-me

# CORS (set * for self-hosted)
CORS_ORIGIN=*

# Server
PORT=7860
HOST=0.0.0.0
