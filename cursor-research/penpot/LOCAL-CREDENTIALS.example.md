# Penpot local credentials (gitignored)

After `create-demo-profile` or registration, record locally (do not commit):

- instance URL (default `http://localhost:9001`)
- demo account email
- demo account login
- team id
- file id
