{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core capability for the main NIL window: window controls, event listen/emit, and the app API the SvelteKit UI already calls.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
